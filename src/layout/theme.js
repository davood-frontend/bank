import { createTheme } from "@mui/material/styles";

export const customTheme = createTheme({
    direction: 'rtl',
    palette: {
        mode: 'light',
    },
    typography: {
        fontFamily: 'Roboto, YekanWeb',
    },
});